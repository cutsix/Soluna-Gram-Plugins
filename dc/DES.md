# dc

查看单个用户所在 DC，或统计当前群组成员的 DC 分布。

## 命令

```text
,dc
,dc force
```

## 说明

- 回复某位用户消息时，显示该用户所在的 `DC`。
- 在私聊中直接使用时，查询当前对象的 `DC`。
- 在群组中直接使用时，统计本群成员 `DC1-DC5` 的分布情况。
- 成员过多时需要使用 `,dc force` 强制执行，且只会遍历前 10k 人。
